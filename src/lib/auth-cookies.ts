/**
 * Constantes de cookie isoladas (sem deps Node-only).
 * Importado pelo middleware (edge runtime) e por lib/auth.ts (Node runtime).
 */
export const ACCESS_COOKIE = "l2i_access";
export const REFRESH_COOKIE = "l2i_refresh";
